# Node-Backend

- createServer es la función con la que creamos un nuevo server (nuestro backend de node en este caso).

```ts
createServer (uno, dos)
```
Uno y dos son sus parámetros, los cuales ya están tipados: uno (IncomingMessage), dos (ServerResponses).
